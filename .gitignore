node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
# SQLite
*.db
*.db-shm
*.db-wal

# Runtime PVC mount (E2E + local dev write here)
/attachments

# Per-environment OpenShift artifacts — created locally per cluster,
# never committed. The committed templates are the .example files and
# the shared deployment.yaml / pvc-*.yaml / route.yaml / service.yaml.
openshift/secret.yaml
openshift/configmap-*.yaml
